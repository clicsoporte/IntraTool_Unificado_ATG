# ==========================================================================================
# GitHub Actions Workflow - Sonar
# ------------------------------------------------------------------------------------------
# Performs SonarCloud static code analysis
# and security scanning for the Servy solution.
# ==========================================================================================

name: sonar
on:
  push:
    branches:
      - main
  # pull_request:
  #   types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build and analyze
    runs-on: windows-latest
    timeout-minutes: 60

    env:
      SONAR_SCANNER_VERSION: "11.2.1"
      MAX_RETRIES: 5 # Maximum retry attempts for resilient network down-stream downloads

    steps:
      # 1. Checkout code first so the custom local action is available
      # (Note: fetch-depth: 0 is retained for complete Git history)
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 #v6.0.3
        with:
          fetch-depth: 0

      # 2. Setup Java
      - name: Set up JDK 17
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 #v5.6.0
        with:
          java-version: 17
          distribution: 'zulu'

      # 3. CRITICAL: Setup .NET using the custom action to satisfy global.json
      - name: Setup .NET
        uses: ./.github/actions/setup-dotnet
        with:
          max_retries: ${{ env.MAX_RETRIES }}

      # 4. Cache Packages
      - name: Cache SonarCloud packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 #v6.1.0
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar-analyzers-v1
          restore-keys: ${{ runner.os }}-sonar-analyzers-

      # 5. Cache Scanner
      - name: Cache SonarCloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 #v6.1.0
        with:
          path: ${{ runner.temp }}\scanner
          key: ${{ runner.os }}-sonar-scanner-${{ env.SONAR_SCANNER_VERSION }}
          restore-keys: ${{ runner.os }}-sonar-scanner-

      # 6. Install / Update Scanner
      - name: Install SonarCloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $scannerDir = "$env:RUNNER_TEMP\scanner"
          if (-not (Test-Path $scannerDir)) { New-Item -Path $scannerDir -ItemType Directory | Out-Null }
          # Utilizing 'update' ensures version switches don't crash when versionless fallbacks populate the tool-path
          dotnet tool update dotnet-sonarscanner --version $env:SONAR_SCANNER_VERSION --tool-path $scannerDir
          if ($LASTEXITCODE -ne 0) { throw "dotnet tool update dotnet-sonarscanner failed (exit $LASTEXITCODE)" }

      # 7. Build and Analyze
      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: pwsh
        run: |
          $scannerDir = "$env:RUNNER_TEMP\scanner"
          & "$scannerDir\dotnet-sonarscanner" begin `
            /k:"aelassas_servy" `
            /o:"aelassas" `
            /d:sonar.token="$env:SONAR_TOKEN" `
            /d:sonar.host.url="https://sonarcloud.io"

          if ($LASTEXITCODE -ne 0) { throw "sonarscanner begin failed (exit $LASTEXITCODE)" }

          dotnet build Servy.sln --configuration Release
          $buildExit = $LASTEXITCODE

          & "$scannerDir\dotnet-sonarscanner" end /d:sonar.token="$env:SONAR_TOKEN"
          $endExit = $LASTEXITCODE

          if ($buildExit -ne 0) { throw "dotnet build failed (exit $buildExit)" }
          if ($endExit  -ne 0) { throw "sonarscanner end failed (exit $endExit)" }
