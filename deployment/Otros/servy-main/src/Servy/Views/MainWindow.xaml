<Window x:Class="Servy.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Servy"
        xmlns:resx="clr-namespace:Servy.Resources"
        xmlns:viewmodels="clr-namespace:Servy.ViewModels"
        xmlns:converters="clr-namespace:Servy.UI.Converters;assembly=Servy.UI"
        xmlns:helpers="clr-namespace:Servy.Helpers"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Servy"
        MinWidth="740"
        MaxWidth="740"
        MinHeight="580"
        Height="680"
        SizeToContent="Manual"
        ResizeMode="CanMinimize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        AutomationProperties.Name="{x:Static resx:Strings.Automation_ConfigMainWindow_Name}"
        AutomationProperties.AutomationId="ConfigMainWindow"
        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ConfigMainWindow_HelpText}">
  <Window.Resources>
    <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />

    <sys:Int32 x:Key="UnlimitedTextBoxLines">2147483647</sys:Int32>

    <!-- Tab Grid Dimensions -->
    <sys:Double x:Key="TabGridMinHeight">380</sys:Double>
    <GridLength x:Key="ColumnWidthMain">390</GridLength>
    <GridLength x:Key="ColumnWidthAction">100</GridLength>

    <!-- Button Dimensions -->
    <sys:Double x:Key="ActionButtonWidth">90</sys:Double>
    <sys:Double x:Key="ActionButtonHeight">22</sys:Double>
    <sys:Double x:Key="BottomButtonWidth">80</sys:Double>
    <sys:Double x:Key="BottomButtonHeight">22</sys:Double>

    <!-- Implicit Action/Browse Button Style for Grid columns -->
    <Style TargetType="Button">
      <Setter Property="Width" Value="{StaticResource ActionButtonWidth}" />
      <Setter Property="Height" Value="{StaticResource ActionButtonHeight}" />
    </Style>

    <!-- Bottom Panel Button Style -->
    <Style x:Key="BottomButtonStyle" TargetType="Button">
      <Setter Property="Width" Value="{StaticResource BottomButtonWidth}" />
      <Setter Property="Height" Value="{StaticResource BottomButtonHeight}" />
      <Setter Property="Margin" Value="5" />
    </Style>

    <!-- Standard Tab Grid Style - Keyed so it doesn't pollute layout Grids -->
    <Style x:Key="TabGridStyle" TargetType="Grid">
      <Setter Property="Margin" Value="20" />
      <Setter Property="HorizontalAlignment" Value="Center" />
      <Setter Property="VerticalAlignment" Value="Top" />
      <Setter Property="MinHeight" Value="{StaticResource TabGridMinHeight}" />
    </Style>

    <!-- Default Single-Line Field Style -->
    <Style TargetType="TextBox">
      <Setter Property="Height" Value="22" />
      <Setter Property="MinHeight" Value="22" />
      <Setter Property="MaxHeight" Value="22" />
      <Setter Property="MaxLines" Value="1" />
      <Setter Property="TextWrapping" Value="NoWrap" />
      <Setter Property="AcceptsReturn" Value="False" />
      <Setter Property="HorizontalScrollBarVisibility" Value="Hidden" />
      <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="SnapsToDevicePixels" Value="False" />
      <Setter Property="UseLayoutRounding" Value="False" />
      <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
      <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
    </Style>

    <!-- Default PasswordBox Field Style -->
    <Style TargetType="PasswordBox">
      <Setter Property="Height" Value="22" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="SnapsToDevicePixels" Value="False" />
      <Setter Property="UseLayoutRounding" Value="False" />
      <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
      <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
    </Style>
  </Window.Resources>

  <DockPanel>
    <Menu DockPanel.Dock="Top"
          Height="28"
          IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
          AutomationProperties.Name="{x:Static resx:Strings.Automation_MainMenu_Name}"
          AutomationProperties.AutomationId="MainMenu"
          AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MainMenu_HelpText}">
      <MenuItem Header="{x:Static resx:Strings.Menu_Export}"
                AutomationProperties.Name="{x:Static resx:Strings.Menu_Export}"
                AutomationProperties.AutomationId="MenuExport"
                AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuExport_HelpText}">
        <MenuItem Header="{x:Static resx:Strings.Menu_ExportXml}"
                  Command="{Binding ExportXmlCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Menu_ExportXml}"
                  AutomationProperties.AutomationId="MenuExportXml"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuExportXml_HelpText}" />
        <MenuItem Header="{x:Static resx:Strings.Menu_ExportJson}"
                  Command="{Binding ExportJsonCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Menu_ExportJson}"
                  AutomationProperties.AutomationId="MenuExportJson"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuExportJson_HelpText}" />
      </MenuItem>

      <MenuItem Header="{x:Static resx:Strings.Menu_Import}"
                AutomationProperties.Name="{x:Static resx:Strings.Menu_Import}"
                AutomationProperties.AutomationId="MenuImport"
                AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuImport_HelpText}">
        <MenuItem Header="{x:Static resx:Strings.Menu_ImportXml}"
                  Command="{Binding ImportXmlCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Menu_ImportXml}"
                  AutomationProperties.AutomationId="MenuImportXml"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuImportXml_HelpText}" />
        <MenuItem Header="{x:Static resx:Strings.Menu_ImportJson}"
                  Command="{Binding ImportJsonCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Menu_ImportJson}"
                  AutomationProperties.AutomationId="MenuImportJson"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuImportJson_HelpText}" />
      </MenuItem>

      <MenuItem Header="{x:Static resx:Strings.Menu_Manager}"
                Command="{Binding ManagerCommand}"
                IsEnabled="{Binding IsManagerAppAvailable}"
                AutomationProperties.Name="{x:Static resx:Strings.Menu_Manager}"
                AutomationProperties.AutomationId="MenuManager"
                AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuManager_HelpText}" />

      <MenuItem Header="{x:Static resx:Strings.Menu_ClearAll}"
                Command="{Binding ClearFormCommand}"
                AutomationProperties.Name="{x:Static resx:Strings.Menu_ClearAll}"
                AutomationProperties.AutomationId="MenuClearAll"
                AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuClearAll_HelpText}" />

      <MenuItem Header="{x:Static resx:Strings.Menu_Help}"
                AutomationProperties.Name="{x:Static resx:Strings.Menu_Help}"
                AutomationProperties.AutomationId="MenuHelp"
                AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuHelp_HelpText}">
        <MenuItem Header="{x:Static resx:Strings.Menu_Doc}"
                  Command="{Binding OpenDocumentationCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Menu_Doc}"
                  AutomationProperties.AutomationId="MenuDoc"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuDoc_HelpText}" />
        <MenuItem Header="{x:Static resx:Strings.Menu_CheckUpdates}"
                  Command="{Binding CheckUpdatesCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Menu_CheckUpdates}"
                  AutomationProperties.AutomationId="MenuCheckUpdates"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuCheckUpdates_HelpText}" />
        <MenuItem Header="{x:Static resx:Strings.Menu_About}"
                  Command="{Binding OpenAboutDialogCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Menu_About}"
                  AutomationProperties.AutomationId="MenuAbout"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_MenuAbout_HelpText}" />
      </MenuItem>
    </Menu>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <TabControl Grid.Row="0"
                  VerticalAlignment="Stretch"
                  IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                  AutomationProperties.Name="{x:Static resx:Strings.Automation_ConfigurationTabControl_Name}"
                  AutomationProperties.AutomationId="ConfigurationTabControl"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ConfigurationTabControl_HelpText}">

        <TabItem Header="{x:Static resx:Strings.Label_MainTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_MainTab}"
                 AutomationProperties.AutomationId="TabItemMain"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemMain_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <TextBlock Grid.Row="0" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ServiceName}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding ServiceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxServiceName_Name}"
                       AutomationProperties.AutomationId="TextBoxServiceName"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxServiceName_HelpText}" />

              <TextBlock Grid.Row="1" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ServiceDisplayName}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding ServiceDisplayName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxServiceDisplayName_Name}"
                       AutomationProperties.AutomationId="TextBoxServiceDisplayName"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxServiceDisplayName_HelpText}" />
              <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ServiceDisplayName}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ServiceDescription}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding ServiceDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxServiceDescription_Name}"
                       AutomationProperties.AutomationId="TextBoxServiceDescription"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxServiceDescription_HelpText}" />

              <TextBlock Grid.Row="4" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessPath}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="4" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding ProcessPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxProcessPath_Name}"
                       AutomationProperties.AutomationId="TextBoxProcessPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxProcessPath_HelpText}" />
              <Button Grid.Row="4" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowseProcessPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowseProcessPath_Name}"
                      AutomationProperties.AutomationId="ButtonBrowseProcessPath"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowseProcessPath_HelpText}" />
              <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessPath}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="6" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartupDirectory}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="6" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding StartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxStartupDirectory_Name}"
                       AutomationProperties.AutomationId="TextBoxStartupDirectory"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxStartupDirectory_HelpText}" />
              <Button Grid.Row="6" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowseStartupDirectoryCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowseStartupDirectory_Name}"
                      AutomationProperties.AutomationId="ButtonBrowseStartupDirectory"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowseStartupDirectory_HelpText}" />
              <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="8" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessParameters}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding ProcessParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="80" MinHeight="80" MaxHeight="80"
                       TextWrapping="Wrap" AcceptsReturn="False"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxProcessParameters_Name}"
                       AutomationProperties.AutomationId="TextBoxProcessParameters"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxProcessParameters_HelpText}" />
              <TextBlock Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessParameters}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="10" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartupType}"
                         Margin="0,5,10,5" />
              <ComboBox Grid.Row="10" Grid.Column="1" Grid.ColumnSpan="2"
                        Margin="0,5"
                        ItemsSource="{Binding StartupTypes}"
                        DisplayMemberPath="DisplayName"
                        SelectedValue="{Binding SelectedStartupType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedValuePath="Value"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_StartupType}"
                        AutomationProperties.AutomationId="ComboBoxStartupType"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ComboBoxStartupType_HelpText}" />

              <TextBlock Grid.Row="11" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessPriority}"
                         Margin="0,5,10,5" />
              <ComboBox Grid.Row="11" Grid.Column="1" Grid.ColumnSpan="2"
                        Margin="0,5"
                        ItemsSource="{Binding ProcessPriorities}"
                        DisplayMemberPath="DisplayName"
                        SelectedValue="{Binding SelectedProcessPriority, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedValuePath="Value"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_ProcessPriority}"
                        AutomationProperties.AutomationId="ComboBoxProcessPriority"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ComboBoxProcessPriority_HelpText}" />

              <TextBlock Grid.Row="12" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_CpuAffinity}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="12" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding CpuAffinity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxCpuAffinity_Name}"
                       AutomationProperties.AutomationId="TextBoxCpuAffinity"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxCpuAffinity_HelpText}" />
              <TextBlock Grid.Row="13" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_CpuAffinity}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="14" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartTimeout}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="14" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding StartTimeout, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxStartTimeout_Name}"
                       AutomationProperties.AutomationId="TextBoxStartTimeout"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxStartTimeout_HelpText}" />
              <TextBlock Grid.Row="15" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StartTimeout}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="16" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StopTimeout}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="16" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding StopTimeout, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxStopTimeout_Name}"
                       AutomationProperties.AutomationId="TextBoxStopTimeout"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxStopTimeout_HelpText}" />
              <TextBlock Grid.Row="17" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StopTimeout}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <CheckBox Grid.Row="18" Grid.Column="1" Grid.ColumnSpan="2"
                        IsChecked="{Binding EnableConsoleUI, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,10"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_EnableConsoleUI}"
                        AutomationProperties.AutomationId="CheckBoxEnableConsoleUI"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxEnableConsoleUI_HelpText}">
                <CheckBox.Content>
                  <TextBlock Text="{x:Static resx:Strings.Label_EnableConsoleUI}" TextWrapping="Wrap" />
                </CheckBox.Content>
              </CheckBox>
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_LoggingTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_LoggingTab}"
                 AutomationProperties.AutomationId="TabItemLogging"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemLogging_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <TextBlock Grid.Row="0" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StdoutPath}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="0" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding StdoutPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxStdoutPath_Name}"
                       AutomationProperties.AutomationId="TextBoxStdoutPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxStdoutPath_HelpText}" />
              <Button Grid.Row="0" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowseStdoutPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowseStdout_Name}"
                      AutomationProperties.AutomationId="ButtonBrowseStdout"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowseStdout_HelpText}" />

              <TextBlock Grid.Row="1" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StderrPath}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="1" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding StderrPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxStderrPath_Name}"
                       AutomationProperties.AutomationId="TextBoxStderrPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxStderrPath_HelpText}" />
              <Button Grid.Row="1" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowseStderrPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowseStderr_Name}"
                      AutomationProperties.AutomationId="ButtonBrowseStderr"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowseStderr_HelpText}" />

              <CheckBox Grid.Row="2" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_EnableSizeRotation}"
                        IsChecked="{Binding EnableSizeRotation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_EnableSizeRotation}"
                        AutomationProperties.AutomationId="CheckBoxEnableSizeRotation"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxEnableSizeRotation_HelpText}" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_RotationSize}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="3" Grid.Column="1"
                       Margin="0,5"
                       IsEnabled="{Binding EnableSizeRotation}"
                       Text="{Binding RotationSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxRotationSize_Name}"
                       AutomationProperties.AutomationId="TextBoxRotationSize"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxRotationSize_HelpText}" />
              <TextBlock Grid.Row="3" Grid.Column="2"
                         Text="{x:Static resx:Strings.Label_RotationSizeUnit}"
                         VerticalAlignment="Center"
                         Margin="5,5,0,5" />

              <CheckBox Grid.Row="4" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_EnableDateRotation}"
                        IsChecked="{Binding EnableDateRotation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_EnableDateRotation}"
                        AutomationProperties.AutomationId="CheckBoxEnableDateRotation"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxEnableDateRotation_HelpText}" />

              <TextBlock Grid.Row="5" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_DateRotationType}"
                         Margin="0,5,10,5" />
              <ComboBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                        Margin="0,5"
                        ItemsSource="{Binding DateRotationTypes}"
                        DisplayMemberPath="DisplayName"
                        SelectedValue="{Binding SelectedDateRotationType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedValuePath="Value"
                        IsEnabled="{Binding EnableDateRotation}"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_DateRotationType}"
                        AutomationProperties.AutomationId="ComboBoxDateRotationType"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ComboBoxDateRotationType_HelpText}" />
              <TextBlock Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_DateRotation}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="7" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_MaxRotations}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       IsEnabled="{Binding EnableRotation}"
                       Text="{Binding MaxRotations, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxMaxRotations_Name}"
                       AutomationProperties.AutomationId="TextBoxMaxRotations"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxMaxRotations_HelpText}" />
              <TextBlock Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_MaxRotations}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <CheckBox Grid.Row="9" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_UseLocalTimeForRotation}"
                        IsChecked="{Binding UseLocalTimeForRotation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_UseLocalTimeForRotation}"
                        AutomationProperties.AutomationId="CheckBoxUseLocalTimeForRotation"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxUseLocalTimeForRotation_HelpText}" />

              <CheckBox Grid.Row="10" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_EnableDebugLogs}"
                        IsChecked="{Binding EnableDebugLogs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_EnableDebugLogs}"
                        AutomationProperties.AutomationId="CheckBoxEnableDebugLogs"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxEnableDebugLogs_HelpText}" />
              <TextBlock Grid.Row="11" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_EnableDebugLogs}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_RecoveryTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_RecoveryTab}"
                 AutomationProperties.AutomationId="TabItemRecovery"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemRecovery_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <CheckBox Grid.Row="0" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_EnableHealthMonitoring}"
                        IsChecked="{Binding EnableHealthMonitoring, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_EnableHealthMonitoring}"
                        AutomationProperties.AutomationId="CheckBoxEnableHealthMonitoring"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxEnableHealthMonitoring_HelpText}" />

              <TextBlock Grid.Row="1" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_HeartbeatInterval}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="1" Grid.Column="1"
                       Margin="0,5"
                       IsEnabled="{Binding EnableHealthMonitoring}"
                       Text="{Binding HeartbeatInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxHeartbeatInterval_Name}"
                       AutomationProperties.AutomationId="TextBoxHeartbeatInterval"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxHeartbeatInterval_HelpText}" />
              <TextBlock Grid.Row="1" Grid.Column="2"
                         Text="{x:Static resx:Strings.Label_Seconds}"
                         VerticalAlignment="Center"
                         Margin="5,5,0,5" />

              <TextBlock Grid.Row="2" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_MaxFailedChecks}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="2" Grid.Column="1"
                       Margin="0,5"
                       IsEnabled="{Binding EnableHealthMonitoring}"
                       Text="{Binding MaxFailedChecks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxMaxFailedChecks_Name}"
                       AutomationProperties.AutomationId="TextBoxMaxFailedChecks"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxMaxFailedChecks_HelpText}" />
              <TextBlock Grid.Row="2" Grid.Column="2"
                         Text="{x:Static resx:Strings.Label_Attempts}"
                         VerticalAlignment="Center"
                         Margin="5,5,0,5" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_RecoveryAction}"
                         Margin="0,5,10,5" />
              <ComboBox Grid.Row="3" Grid.Column="1"
                        Margin="0,5"
                        ItemsSource="{Binding RecoveryActions}"
                        DisplayMemberPath="DisplayName"
                        SelectedValue="{Binding SelectedRecoveryAction, Mode=TwoWay}"
                        SelectedValuePath="Value"
                        IsEnabled="{Binding EnableHealthMonitoring}"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_RecoveryAction}"
                        AutomationProperties.AutomationId="ComboBoxRecoveryAction"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ComboBoxRecoveryAction_HelpText}" />
              <Button Grid.Row="3" Grid.Column="2"
                      Command="{Binding ShowRecoveryActionHelpCommand}"
                      Margin="6,0,0,0" Padding="0"
                      Width="16" Height="16"
                      Cursor="Hand" Background="Transparent" BorderThickness="0"
                      HorizontalAlignment="Left" VerticalAlignment="Center"
                      ToolTip="{x:Static resx:Strings.Tooltip_RecoveryActionHelp}"
                      AutomationProperties.Name="{x:Static resx:Strings.Tooltip_RecoveryActionHelp}"
                      AutomationProperties.AutomationId="ButtonRecoveryActionHelp"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonRecoveryActionHelp_HelpText}">
                <Button.Template>
                  <ControlTemplate TargetType="Button">
                    <Border Background="Transparent">
                      <Path Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4M11,16.5H13V18.5H11V16.5M12,6.5A3.5,3.5 0 0,0 8.5,10H10.5A1.5,1.5 0 0,1 12,8.5A1.5,1.5 0 0,1 13.5,10C13.5,11.5 11.25,11.25 11.25,13.5H12.75C12.75,12 15,11.75 15,10A3.5,3.5 0 0,0 12,6.5Z"
                            Fill="#0078D4" Stretch="Uniform" />
                    </Border>
                  </ControlTemplate>
                </Button.Template>
              </Button>

              <CheckBox Grid.Row="4" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_RecoveryOnCleanExit}"
                        IsEnabled="{Binding EnableHealthMonitoring}"
                        IsChecked="{Binding RecoveryOnCleanExit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_RecoveryOnCleanExit}"
                        AutomationProperties.AutomationId="CheckBoxRecoveryOnCleanExit"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxRecoveryOnCleanExit_HelpText}" />
              <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_RecoveryOnCleanExit}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="6" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_MaxRestartAttempts}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="6" Grid.Column="1"
                       Margin="0,5"
                       IsEnabled="{Binding EnableHealthMonitoring}"
                       Text="{Binding MaxRestartAttempts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxMaxRestartAttempts_Name}"
                       AutomationProperties.AutomationId="TextBoxMaxRestartAttempts"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxMaxRestartAttempts_HelpText}" />
              <TextBlock Grid.Row="6" Grid.Column="2"
                         Text="{x:Static resx:Strings.Label_Attempts}"
                         VerticalAlignment="Center"
                         Margin="5,5,0,5" />
              <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_MaxRestartAttempts}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" Margin="0,0,0,5" />

              <TextBlock Grid.Row="8" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_HeartbeatUrl}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="8" Grid.Column="1"
                       Margin="0,5"
                       IsEnabled="{Binding EnableHealthMonitoring}"
                       Text="{Binding HeartbeatUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxHeartbeatUrl_Name}"
                       AutomationProperties.AutomationId="TextBoxHeartbeatUrl"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxHeartbeatUrl_HelpText}" />

              <TextBlock Grid.Row="9" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_HeartbeatUrlTimeoutSeconds}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="9" Grid.Column="1"
                       Margin="0,5"
                       IsEnabled="{Binding EnableHealthMonitoring}"
                       Text="{Binding HeartbeatUrlTimeoutSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxHeartbeatUrlTimeoutSeconds_Name}"
                       AutomationProperties.AutomationId="TextBoxHeartbeatUrlTimeoutSeconds"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxHeartbeatUrlTimeoutSeconds_HelpText}" />
              <TextBlock Grid.Row="9" Grid.Column="2"
                         Text="{x:Static resx:Strings.Label_Seconds}"
                         VerticalAlignment="Center"
                         Margin="5,5,0,5" />

              <CheckBox Grid.Row="10" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_EnableHeartbeatUrlFlags}"
                        IsEnabled="{Binding EnableHealthMonitoring}"
                        IsChecked="{Binding EnableHeartbeatUrlFlags, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_EnableHeartbeatUrlFlags}"
                        AutomationProperties.AutomationId="CheckBoxEnableHeartbeatUrlFlags"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxEnableHeartbeatUrlFlags_HelpText}" />

              <TextBlock Grid.Row="11" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_FailureProgram}"
                         Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                         TextWrapping="Wrap" Margin="0,0,0,5" />

              <TextBlock Grid.Row="12" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="12" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding FailureProgramPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxFailureProgramPath_Name}"
                       AutomationProperties.AutomationId="TextBoxFailureProgramPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxFailureProgramPath_HelpText}" />
              <Button Grid.Row="12" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowseFailureProgramPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowseFailureProgramPath_Name}"
                      AutomationProperties.AutomationId="ButtonBrowseFailureProgramPath"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowseFailureProgramPath_HelpText}" />
              <TextBlock Grid.Row="13" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessPath}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="14" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartupDirectory}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="14" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding FailureProgramStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxFailureProgramStartupDir_Name}"
                       AutomationProperties.AutomationId="TextBoxFailureProgramStartupDir"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxFailureProgramStartupDir_HelpText}" />
              <Button Grid.Row="14" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowseFailureProgramStartupDirectoryCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowseFailureProgramDir_Name}"
                      AutomationProperties.AutomationId="ButtonBrowseFailureProgramDir"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowseFailureProgramDir_HelpText}" />
              <TextBlock Grid.Row="15" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_FailureProgramStartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
              <TextBlock Grid.Row="16" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="17" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessParameters}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="17" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding FailureProgramParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="80" MinHeight="80" MaxHeight="80"
                       TextWrapping="Wrap" AcceptsReturn="False"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxFailureProgramParams_Name}"
                       AutomationProperties.AutomationId="TextBoxFailureProgramParams"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxFailureProgramParams_HelpText}" />
              <TextBlock Grid.Row="18" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessParameters}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_AdvancedTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_AdvancedTab}"
                 AutomationProperties.AutomationId="TabItemAdvanced"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemAdvanced_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <TextBlock Grid.Row="0" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_EnvironmentVariables}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding EnvironmentVariables, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="120" MinHeight="120" MaxHeight="120"
                       AcceptsReturn="True" TextWrapping="Wrap"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxEnvVars_Name}"
                       AutomationProperties.AutomationId="TextBoxEnvVars"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxEnvVars_HelpText}" />
              <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_EnvironmentVariables}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="2" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ServiceDependencies}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding ServiceDependencies, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="120" MinHeight="120" MaxHeight="120"
                       AcceptsReturn="True" TextWrapping="Wrap"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxServiceDependencies_Name}"
                       AutomationProperties.AutomationId="TextBoxServiceDependencies"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxServiceDependencies_HelpText}" />
              <TextBlock Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ServiceDependencies}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_LogOnTab}"
         AutomationProperties.Name="{x:Static resx:Strings.Label_LogOnTab}"
         AutomationProperties.AutomationId="TabItemLogOn"
         AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemLogOn_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
              </Grid.ColumnDefinitions>

              <RadioButton Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Margin="0,0,0,10"
                   Content="{x:Static resx:Strings.Label_RunAsLocalSystem}"
                   IsChecked="{Binding RunAsLocalSystem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   AutomationProperties.Name="{x:Static resx:Strings.Label_RunAsLocalSystem}"
                   AutomationProperties.AutomationId="RadioButtonLocalSystem"
                   AutomationProperties.HelpText="{x:Static resx:Strings.Automation_RadioButtonLocalSystem_HelpText}" />

              <RadioButton Grid.Row="1" Grid.Column="0"
                   Content="{x:Static resx:Strings.Label_UserAccount}"
                   IsChecked="{Binding RunAsLocalSystem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource InverseBooleanConverter}}"
                   AutomationProperties.Name="{x:Static resx:Strings.Label_UserAccount}"
                   AutomationProperties.AutomationId="RadioButtonUserAccount"
                   AutomationProperties.HelpText="{x:Static resx:Strings.Automation_RadioButtonUserAccount_HelpText}" />

              <TextBox Grid.Row="1" Grid.Column="1"
                       Margin="0,0,0,5"
                       IsEnabled="{Binding RunAsLocalSystem, Converter={StaticResource InverseBooleanConverter}}"
                       Text="{Binding UserAccount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxUserAccount_Name}"
                       AutomationProperties.AutomationId="TextBoxUserAccount"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxUserAccount_HelpText}" />

              <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                 Text="{x:Static resx:Strings.Info_UserAccount}"
                 Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                 TextWrapping="Wrap" Margin="0,0,0,10" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                 Text="{x:Static resx:Strings.Label_Password}"
                 Margin="0,5,10,5" VerticalAlignment="Center" />

              <PasswordBox Grid.Row="3" Grid.Column="1"
                   Margin="0,0,0,10"
                   IsEnabled="{Binding RunAsLocalSystem, Converter={StaticResource InverseBooleanConverter}}"
                   helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   AutomationProperties.Name="{x:Static resx:Strings.Label_Password}"
                   AutomationProperties.AutomationId="PasswordBoxUser"
                   AutomationProperties.HelpText="{x:Static resx:Strings.Automation_PasswordBoxUser_HelpText}" />

              <TextBlock Grid.Row="4" Grid.Column="0"
                 Text="{x:Static resx:Strings.Label_ConfirmPassword}"
                 Margin="0,5,10,5" VerticalAlignment="Center" />

              <PasswordBox Grid.Row="4" Grid.Column="1"
                   Margin="0,0,0,10"
                   IsEnabled="{Binding RunAsLocalSystem, Converter={StaticResource InverseBooleanConverter}}"
                   helpers:PasswordBoxHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   AutomationProperties.Name="{x:Static resx:Strings.Label_ConfirmPassword}"
                   AutomationProperties.AutomationId="PasswordBoxConfirm"
                   AutomationProperties.HelpText="{x:Static resx:Strings.Automation_PasswordBoxConfirm_HelpText}" />

              <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                 Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                 TextWrapping="Wrap" Margin="0,5,0,10">
                  <Run Text="{x:Static resx:Strings.Info_SecurityHardeningNotice}" />
                  <Hyperlink Command="{Binding OpenSecurityHardeningGuideCommand}">
                    <Run Text="{x:Static resx:Strings.Link_SecurityHardeningGuide}" />
                  </Hyperlink>
              </TextBlock>
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_PreLaunchTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_PreLaunchTab}"
                 AutomationProperties.AutomationId="TabItemPreLaunch"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemPreLaunch_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PreLaunch}"
                         Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                         TextWrapping="Wrap" Margin="5,0,0,5" />

              <TextBlock Grid.Row="1" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="1" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PreLaunchExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchProcessPath_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchProcessPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchProcessPath_HelpText}" />
              <Button Grid.Row="1" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePreLaunchProcessPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchProcessPath_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePreLaunchProcessPath"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchProcessPath_HelpText}" />
              <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessPath}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartupDirectory}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="3" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PreLaunchStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchStartupDir_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchStartupDir"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchStartupDir_HelpText}" />
              <Button Grid.Row="3" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePreLaunchStartupDirectoryCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchStartupDir_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePreLaunchStartupDir"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchStartupDir_HelpText}" />
              <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PreLaunchStartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
              <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="6" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessParameters}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PreLaunchParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="80" MinHeight="80" MaxHeight="80"
                       TextWrapping="Wrap" AcceptsReturn="False"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchParams_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchParams"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchParams_HelpText}" />
              <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessParameters}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="8" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_EnvironmentVariables}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PreLaunchEnvironmentVariables, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="100" MinHeight="100" MaxHeight="100"
                       AcceptsReturn="True" TextWrapping="Wrap"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchEnvVars_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchEnvVars"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchEnvVars_HelpText}" />
              <TextBlock Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_EnvironmentVariables}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="10" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StdoutPath}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="10" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PreLaunchStdoutPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchStdout_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchStdout"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchStdout_HelpText}" />
              <Button Grid.Row="10" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePreLaunchStdoutPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchStdout_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePreLaunchStdout"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchStdout_HelpText}" />

              <TextBlock Grid.Row="11" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StderrPath}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="11" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PreLaunchStderrPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchStderr_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchStderr"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchStderr_HelpText}" />
              <Button Grid.Row="11" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePreLaunchStderrPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchStderr_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePreLaunchStderr"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePreLaunchStderr_HelpText}" />

              <TextBlock Grid.Row="12" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_PreLaunchTimeoutSeconds}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="12" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PreLaunchTimeoutSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchTimeout_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchTimeout"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchTimeout_HelpText}" />
              <TextBlock Grid.Row="13" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PreLaunchTimeout}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="14" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_PreLaunchRetryAttempts}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="14" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PreLaunchRetryAttempts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreLaunchRetries_Name}"
                       AutomationProperties.AutomationId="TextBoxPreLaunchRetries"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreLaunchRetries_HelpText}" />

              <CheckBox Grid.Row="15" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_PreLaunchIgnoreFailure}"
                        IsChecked="{Binding PreLaunchIgnoreFailure, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_PreLaunchIgnoreFailure}"
                        AutomationProperties.AutomationId="CheckBoxPreLaunchIgnoreFailure"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxPreLaunchIgnoreFailure_HelpText}" />
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_PostLaunchTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_PostLaunchTab}"
                 AutomationProperties.AutomationId="TabItemPostLaunch"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemPostLaunch_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PostLaunch}"
                         Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                         TextWrapping="Wrap" Margin="5,0,0,5" />

              <TextBlock Grid.Row="1" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="1" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PostLaunchExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPostLaunchPath_Name}"
                       AutomationProperties.AutomationId="TextBoxPostLaunchPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPostLaunchPath_HelpText}" />
              <Button Grid.Row="1" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePostLaunchProcessPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePostLaunchPath_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePostLaunchPath"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePostLaunchPath_HelpText}" />
              <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessPath}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartupDirectory}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="3" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PostLaunchStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPostLaunchDir_Name}"
                       AutomationProperties.AutomationId="TextBoxPostLaunchDir"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPostLaunchDir_HelpText}" />
              <Button Grid.Row="3" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePostLaunchStartupDirectoryCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePostLaunchDir_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePostLaunchDir"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePostLaunchDir_HelpText}" />
              <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PostLaunchStartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
              <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="6" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessParameters}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PostLaunchParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="80" MinHeight="80" MaxHeight="80"
                       TextWrapping="Wrap" AcceptsReturn="False"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPostLaunchParams_Name}"
                       AutomationProperties.AutomationId="TextBoxPostLaunchParams"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPostLaunchParams_HelpText}" />
              <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessParameters}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_PreStopTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_PreStopTab}"
                 AutomationProperties.AutomationId="TabItemPreStop"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemPreStop_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PreStop}"
                         Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                         TextWrapping="Wrap" Margin="5,0,0,5" />

              <TextBlock Grid.Row="1" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="1" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PreStopExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreStopPath_Name}"
                       AutomationProperties.AutomationId="TextBoxPreStopPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreStopPath_HelpText}" />
              <Button Grid.Row="1" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePreStopProcessPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePreStopPath_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePreStopPath"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePreStopPath_HelpText}" />
              <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessPath}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartupDirectory}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="3" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PreStopStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreStopDir_Name}"
                       AutomationProperties.AutomationId="TextBoxPreStopDir"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreStopDir_HelpText}" />
              <Button Grid.Row="3" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePreStopStartupDirectoryCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePreStopDir_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePreStopDir"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePreStopDir_HelpText}" />
              <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PreStopStartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
              <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="6" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessParameters}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PreStopParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="80" MinHeight="80" MaxHeight="80"
                       TextWrapping="Wrap" AcceptsReturn="False"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreStopParams_Name}"
                       AutomationProperties.AutomationId="TextBoxPreStopParams"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreStopParams_HelpText}" />
              <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessParameters}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="8" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_PreStopTimeoutSeconds}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PreStopTimeoutSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPreStopTimeout_Name}"
                       AutomationProperties.AutomationId="TextBoxPreStopTimeout"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPreStopTimeout_HelpText}" />
              <TextBlock Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PreStopTimeout}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <CheckBox Grid.Row="10" Grid.Column="1"
                        Content="{x:Static resx:Strings.Label_PreStopLogAsError}"
                        IsChecked="{Binding PreStopLogAsError, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,5"
                        AutomationProperties.Name="{x:Static resx:Strings.Label_PreStopLogAsError}"
                        AutomationProperties.AutomationId="CheckBoxPreStopLogAsError"
                        AutomationProperties.HelpText="{x:Static resx:Strings.Automation_CheckBoxPreStopLogAsError_HelpText}" />
            </Grid>
          </ScrollViewer>
        </TabItem>

        <TabItem Header="{x:Static resx:Strings.Label_PostStopTab}"
                 AutomationProperties.Name="{x:Static resx:Strings.Label_PostStopTab}"
                 AutomationProperties.AutomationId="TabItemPostStop"
                 AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TabItemPostStop_HelpText}">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Style="{StaticResource TabGridStyle}">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{StaticResource ColumnWidthMain}" />
                <ColumnDefinition Width="{StaticResource ColumnWidthAction}" />
              </Grid.ColumnDefinitions>

              <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PostStop}"
                         Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                         TextWrapping="Wrap" Margin="5,0,0,5" />

              <TextBlock Grid.Row="1" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="1" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PostStopExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPostStopPath_Name}"
                       AutomationProperties.AutomationId="TextBoxPostStopPath"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPostStopPath_HelpText}" />
              <Button Grid.Row="1" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePostStopProcessPathCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePostStopPath_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePostStopPath"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePostStopPath_HelpText}" />
              <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessPath}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="3" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_StartupDirectory}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="3" Grid.Column="1"
                       Margin="0,5"
                       Text="{Binding PostStopStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPostStopDir_Name}"
                       AutomationProperties.AutomationId="TextBoxPostStopDir"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPostStopDir_HelpText}" />
              <Button Grid.Row="3" Grid.Column="2"
                      Content="{x:Static resx:Strings.Button_Browse}"
                      Command="{Binding BrowsePostStopStartupDirectoryCommand}"
                      Margin="5,5,0,5" HorizontalAlignment="Left"
                      AutomationProperties.Name="{x:Static resx:Strings.Automation_ButtonBrowsePostStopDir_Name}"
                      AutomationProperties.AutomationId="ButtonBrowsePostStopDir"
                      AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonBrowsePostStopDir_HelpText}" />
              <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_PostStopStartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
              <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_StartupDirectory}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />

              <TextBlock Grid.Row="6" Grid.Column="0"
                         Text="{x:Static resx:Strings.Label_ProcessParameters}"
                         Margin="0,5,10,5" />
              <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="0,5"
                       Text="{Binding PostStopParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Height="80" MinHeight="80" MaxHeight="80"
                       TextWrapping="Wrap" AcceptsReturn="False"
                       MaxLines="{StaticResource UnlimitedTextBoxLines}" VerticalContentAlignment="Top"
                       VerticalScrollBarVisibility="Auto"
                       AutomationProperties.Name="{x:Static resx:Strings.Automation_TextBoxPostStopParams_Name}"
                       AutomationProperties.AutomationId="TextBoxPostStopParams"
                       AutomationProperties.HelpText="{x:Static resx:Strings.Automation_TextBoxPostStopParams_HelpText}" />
              <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{x:Static resx:Strings.Info_ProcessParameters}"
                         Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                         TextWrapping="Wrap" />
            </Grid>
          </ScrollViewer>
        </TabItem>
      </TabControl>

      <Grid Grid.Row="1" Margin="0,10" VerticalAlignment="Bottom">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="{x:Static resx:Strings.Label_RequiredFields}"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   VerticalAlignment="Center"
                   Margin="10,0,0,0"
                   AutomationProperties.Name="{x:Static resx:Strings.Automation_RequiredFieldsNote_Name}" />

        <UniformGrid Grid.Column="1"
                     Rows="1" Columns="5"
                     Margin="0,0,10,0">
          <Button Content="{x:Static resx:Strings.Button_Install}"
                  Style="{StaticResource BottomButtonStyle}"
                  Command="{Binding InstallCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Button_Install}"
                  AutomationProperties.AutomationId="ButtonInstall"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonInstall_HelpText}" />
          <Button Content="{x:Static resx:Strings.Button_Uninstall}"
                  Style="{StaticResource BottomButtonStyle}"
                  Command="{Binding UninstallCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Button_Uninstall}"
                  AutomationProperties.AutomationId="ButtonUninstall"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonUninstall_HelpText}" />
          <Button Content="{x:Static resx:Strings.Button_Start}"
                  Style="{StaticResource BottomButtonStyle}"
                  Command="{Binding StartCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Button_Start}"
                  AutomationProperties.AutomationId="ButtonStart"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonStart_HelpText}" />
          <Button Content="{x:Static resx:Strings.Button_Stop}"
                  Style="{StaticResource BottomButtonStyle}"
                  Command="{Binding StopCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Button_Stop}"
                  AutomationProperties.AutomationId="ButtonStop"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonStop_HelpText}" />
          <Button Content="{x:Static resx:Strings.Button_Restart}"
                  Style="{StaticResource BottomButtonStyle}"
                  Command="{Binding RestartCommand}"
                  AutomationProperties.Name="{x:Static resx:Strings.Button_Restart}"
                  AutomationProperties.AutomationId="ButtonRestart"
                  AutomationProperties.HelpText="{x:Static resx:Strings.Automation_ButtonRestart_HelpText}" />
        </UniformGrid>
      </Grid>
    </Grid>
  </DockPanel>
</Window>
